# This file controls the behavior of Trunk: https://docs.trunk.io/cli
# To learn more about the format of this file, see https://docs.trunk.io/reference/trunk-yaml
version: 0.1
cli:
  version: 1.25.0
# Trunk provides extensibility via plugins. (https://docs.trunk.io/plugins)
plugins:
  sources:
    - id: trunk
      ref: v1.11.0
      uri: https://github.com/trunk-io/plugins
# Many linters and tools depend on runtimes - configure them here. (https://docs.trunk.io/runtimes)
runtimes:
  enabled:
    - node@22.16.0
    - python@3.14.4
# This is the section where you manage your linters. (https://docs.trunk.io/check/configuration)
lint:
  disabled:
    - bandit
    - checkov
    - git-diff-check
    - isort
    - pinact
    # trunk's dotnet-format plugin hardcodes dotnet SDK 7.0.400 (with roll-forward disabled) and
    # has no way to pin a different version. This repo targets net10.0, so dotnet-format's bundled
    # Roslyn can never load this project's analyzers (Microsoft.CodeAnalysis version mismatch) -
    # it is not a shell/PATH issue and cannot be fixed via trunk.yaml config.
    - dotnet-format
  enabled:
    - actionlint@1.7.12
    - black@26.5.1
    - markdownlint@0.49.1
    - oxipng@10.2.0
    - prettier@3.9.6
    - ruff@0.16.5
    - taplo@0.10.0
    - trufflehog@3.97.1
    - yamllint@1.38.0
actions:
  disabled:
    - trunk-announce
    - trunk-check-pre-push
    - trunk-fmt-pre-commit
  enabled:
    - trunk-upgrade-available
