[project]
name = "mosaicshell"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "S Uddin", email = "uairhahs@outlook.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pillow>=12.3.0",
]

[project.scripts]
mosaicshell = "mosaicshell:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
