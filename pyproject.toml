[project]
name = "mosaicshell"
version = "0.1.0"
description = "Python scripts Scaffolding for MosaicShell"
readme = "README.md"
authors = [{ name = "S Uddin", email = "uairhahs@gmail.com" }]
requires-python = ">=3.11"
dependencies = ["pillow>=12.3.0"]

[tool.uv]
package = false

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
