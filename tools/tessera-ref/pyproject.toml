[project]
name = "tessera-ref"
version = "0.1.0"
description = "Pillow-based statistical analysis and annotation for Tessera flyout reference crops"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pillow>=10.0",
]

[tool.uv]
package = false
